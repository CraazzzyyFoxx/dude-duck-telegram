version: '3.8'

services:
#  redis:
#    image: redis:alpine
#    restart: always
  app:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    command: uvicorn main:app --host 0.0.0.0 --port ${PORT}
    ports:
      - "${PORT}:${PORT}"