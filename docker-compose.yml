version: '3.8'

services:
  redis:
    image: redis:alpine
    restart: always
  app:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    command: gunicorn main:app --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:${PORT}
    ports:
      - "${PORT}:${PORT}"